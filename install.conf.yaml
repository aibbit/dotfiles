- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim : vim/vim
    ~/.vimrc : vim/.vimrc

# - create:
#     - ~/downloads

- shell:
  - [git submodule update --init --recursive, Installing submodules]
