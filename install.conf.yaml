- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim : vim/vim
    ~/.vimrc : vim/.vimrc
    ~/.tmux.conf : tmux/tmux.conf

# - create:
#     - ~/Downloads
#     - ~/Temp
#     - ~/Documents

- shell:
  - [git submodule update --init --recursive, Installing submodules]
